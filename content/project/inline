{
 "cells": [
  {
   "cell_type": "code",
   "execution_count": 1,
   "id": "2a0a21c5",
   "metadata": {},
   "outputs": [],
   "source": [
    "import numpy as np\n",
    "import matplotlib.pyplot as plt"
   ]
  },
  {
   "cell_type": "code",
   "execution_count": 2,
   "id": "514db454",
   "metadata": {},
   "outputs": [],
   "source": [
    "x = [0]\n",
    "y = [0]\n",
    "\n",
    "plt.figure()\n",
    "plt.plot(x, y, 'ro', markersize=10)\n",
    "plt.xlim(-1, 1)\n",
    "plt.ylim(-1, 1)\n",
    "plt.show()"
   ]
  },
  {
   "cell_type": "code",
   "execution_count": 3,
   "id": "2ecc7484",
   "metadata": {},
   "outputs": [],
   "source": [
    "x = [0]\n",
    "y = [0]\n",
    "\n",
    "def update_plot():\n",
    "    x += 1\n",
    "    plt.figure()\n",
    "    plt.plot(x, y, 'ro', markersize=10)\n",
    "    plt.xlim(-1, 1)\n",
    "    plt.ylim(-1, 1)\n",
    "\n",
    "plt.animate(update_plot, frames=10, interval=1000)"
   ]
  },
  {
   "cell_type": "code",
   "execution_count": 4,
   "id": "80d02a06",
   "metadata": {},
   "outputs": [],
   "source": [
    "from matplotlib.animation import FuncAnimation\n",
    "\n",
    "% notebook inline\n",
    "\n",
    "x = [0]\n",
    "y = [0]\n",
    "\n",
    "def update_plot():\n",
    "    x += 1\n",
    "    plt.figure()\n",
    "    plt.plot(x, y, 'ro', markersize=10)\n",
    "    plt.xlim(-1, 1)\n",
    "    plt.ylim(-1, 1)\n",
    "\n",
    "ani = FuncAnimation(update_plot, frames=10, interval=1000)"
   ]
  },
  {
   "cell_type": "code",
   "execution_count": 5,
   "id": "490e95d2",
   "metadata": {},
   "outputs": [],
   "source": [
    "from matplotlib.animation import FuncAnimation\n",
    "\n",
    "%notebook inline\n",
    "\n",
    "x = [0]\n",
    "y = [0]\n",
    "\n",
    "def update_plot():\n",
    "    x += 1\n",
    "    plt.figure()\n",
    "    plt.plot(x, y, 'ro', markersize=10)\n",
    "    plt.xlim(-1, 1)\n",
    "    plt.ylim(-1, 1)\n",
    "\n",
    "ani = FuncAnimation(update_plot, frames=10, interval=1000)"
   ]
  },
  {
   "cell_type": "code",
   "execution_count": 6,
   "id": "e0a0c1e6",
   "metadata": {},
   "outputs": [],
   "source": [
    "from matplotlib.animation import FuncAnimation\n",
    "\n",
    "%notebook inline\n",
    "\n",
    "x = [0]\n",
    "y = [0]\n",
    "\n",
    "fig = plt.figure()\n",
    "def update_plot():\n",
    "    x += 1\n",
    "    \n",
    "    plt.plot(x, y, 'ro', markersize=10)\n",
    "    plt.xlim(-1, 1)\n",
    "    plt.ylim(-1, 1)\n",
    "\n",
    "ani = FuncAnimation(update_plot, frames=10, interval=1000)"
   ]
  }
 ],
 "metadata": {},
 "nbformat": 4,
 "nbformat_minor": 5
}
